name: CI

on:
  pull_request:

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    name: Prepare P5-004
    runs-on: ubuntu-latest
    steps:
      - name: Check out P5-004 branch
        uses: actions/checkout@v4
        with:
          ref: P5-004_daemon_dbus_service

      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.95.0 --profile minimal --component rustfmt,clippy

      - name: Select pinned Rust toolchain
        run: rustup override set 1.95.0

      - name: Refresh lockfile and formatting
        run: |
          cargo check --workspace
          cargo fmt --all

      - name: Commit generated source and lockfile changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Cargo.lock apps/daemon
          if git diff --cached --quiet; then
            echo "No generated changes to commit"
          else
            git commit -m "chore: prepare P5-004 generated state"
            git push origin HEAD:P5-004_daemon_dbus_service
          fi
