[package]
name = "blueroute-client"
description = "Reusable client for the BlueRoute daemon API."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
async-io = "2.6.0"
blueroute-core = { path = "../blueroute-core" }
blueroute-protocol = { path = "../blueroute-protocol" }
futures-lite = "2.6.0"
zbus = { version = "5.19.0", default-features = false, features = ["async-io"] }

[dev-dependencies]
blueroute-daemon = { path = "../../apps/daemon" }

[lints]
workspace = true
